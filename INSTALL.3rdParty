Library dependencies of strusPattern:
	- boost (headers and -lboost_system)
	- ragel statemachine compiler for hyperscan
	- tre library for post matching of regular expressions

Ubuntu:
	sudo apt-get install liboost-all-dev
	sudo apt-get install ragel
	sudo apt-get install libtre-dev

